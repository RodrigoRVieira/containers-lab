FROM ubuntu:18.04

LABEL Version="1.0"
LABEL Author="Rodrigo Riccitelli Vieira"
LABEL Purpose="Custom Ubuntu image with some network tools installed"

RUN apt-get update && apt-get install iputils-ping curl telnet -y